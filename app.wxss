/**app.wxss**/
@import "./styles/iconfont.wxss";

/* 微信小程序中，不支持通配符 */
page, view, text, swiper, swiper-item, image, navigator {
  padding: 0;
  margin: 0;
  box-sizing: border-box;
}

/* 微信小程序默认支持CSS预处理 */
page {
  /* 主题色 */
  --themeColor: #eb4450;
  /* 统一字体大小，假设设计稿是375px */
  font-size: 28rpx;
}